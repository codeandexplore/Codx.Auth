
@model ClientRedirectUrisDetailsViewModel

@{
    ViewData["Title"] = "Client Redirect URIs";
}

<h1>Client Redirect URIs</h1>

<div class="row">
    <div class="col-lg-12">
        <input type="hidden" asp-for="ClientId" />
        <label>ClientID: </label> @Model.ClientIdString <br />
        <label>Client Name: </label> @Model.ClientName <br />
    </div>
</div>

<a asp-action="Add" asp-route-clientid="@Model.ClientId">Add</a>

<div class="row">
    <div class="col-lg-12">
        <table class="table table-bordered">
            <tr>
                <th>Redirect URI</th>
                <th></th>
            </tr>

            @for (var i = 0; i < Model.RedirectUris.Count; i++)
            {
                var row = Model.RedirectUris[i];
                <tr>
                    <td>@row.RedirectUri</td>
                    <td>
                        <a asp-action="Delete" asp-route-id="@row.Id" asp-route-clientid="@Model.ClientId">Delete</a>
                    </td>
                </tr>
            }

        </table>
    </div>
</div>