
@model UserClaimsDetailsViewModel

@{
    ViewData["Title"] = "User Claims";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index","Home")">Home</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("Index","Users")">Users</a></li>
        <li class="breadcrumb-item active" aria-current="page">User Claims</li>
    </ol>
</nav>


<div class="row">
    <div class="col-lg-12">

        <div class="card">
            <div class="card-body">

                <div class="row">
                    <div class="col-lg-12">
                        <input type="hidden" asp-for="UserId" />

                        <div class="form-group">
                            <label>Username</label>
                            <input asp-for="@Model.Username" class="form-control" disabled />
                        </div>
                        <div class="form-group">
                            <label>Email</label>
                            <input asp-for="@Model.Email" class="form-control" disabled />
                        </div>

                    </div>
                </div>

            </div>
        </div>

        <div class="card">
            <div class="card-header">
                <a asp-action="Add" asp-route-userid="@Model.UserId" class="btn btn-outline-primary"><i class="fa fa-plus"></i> &nbsp; Add</a>
            </div>
            <div class="card-body">

                <div class="row">
                    <div class="col-lg-12">
                        <table class="table table-bordered">
                            <tr>
                                <th>Type</th>
                                <th>Value</th>
                                <th></th>
                            </tr>

                            @for (var i = 0; i < Model.Claims.Count; i++)
                            {
                                var row = Model.Claims[i];
                                <tr>
                                    <td>@row.ClaimType</td>
                                    <td>@row.ClaimValue</td>
                                    <td>
                                        <a asp-action="Delete" asp-route-claimid="@row.ClaimId" asp-route-userid="@Model.UserId" class="btn btn-outline-danger">Delete</a>
                                    </td>
                                </tr>
                            }

                        </table>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
